{#
/**
 * @file
 * Menu DETG — navegação principal no estilo dos mockups html_DETG.
 #}
{% import _self as menus %}
{{ menus.menu_links(items, attributes, 0) }}

{% macro menu_links(items, attributes, menu_level) %}
  {% import _self as menus %}
  {% if items %}
    {% if menu_level == 0 %}
      <ul class="nav-links">
    {% else %}
      <ul class="sub-menu">
    {% endif %}
    {% for item in items %}
      {% set url_str = item.url.toString %}
      {% set is_home = url_str == '/' or url_str == path('<front>') or item.title|render|striptags|trim|lower in ['início', 'inicio', 'home'] %}
      {%
        set item_classes = [
          'nav-menu-item',
          item.is_expanded ? 'nav-menu-item--expanded',
          item.is_collapsed ? 'nav-menu-item--collapsed',
          item.in_active_trail ? 'nav-menu-item--active-trail',
          is_home ? 'nav-menu-item--home',
        ]
      %}
      <li{{ item.attributes.addClass(item_classes) }}>
        <a href="{{ url_str }}" class="nav-item{% if item.in_active_trail %} is-active{% endif %}{% if is_home %} nav-item--home{% endif %}">
          {{ item.title }}
        </a>
        {% if item.below %}
          {{ menus.menu_links(item.below, attributes, menu_level + 1) }}
        {% endif %}
      </li>
    {% endfor %}
    </ul>
  {% endif %}
{% endmacro %}
